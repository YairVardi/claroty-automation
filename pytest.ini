[pytest]
addopts = --junitxml=artifacts/junit.xml
testpaths = tests
markers =
    smoke: critical smoke coverage for the public website
    regression: broader stable regression coverage
    navigation: header, footer, link, and menu behavior
    form: form visibility and validation behavior
    responsive: mobile and responsive navigation behavior
